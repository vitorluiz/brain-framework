#!/usr/bin/env python3
"""CLI entry point para o Brain Tool — instalação via pip install brain-tool."""

import sys
from brain_tool.brain_tool import main

if __name__ == "__main__":
    sys.exit(main())
